name: rasedi_flutter_sdk_example
description: Demonstrates how to use the rasedi_flutter_sdk plugin.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  rasedi_flutter_sdk:
    path: ../
